@model IEnumerable<Rassef.ViewModels.Administration.DriverListVM>
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>رصيف — لوحة التحكم (السائقين المسجلين)</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Arabic:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/dashboard.css">
</head>
<body>
    <div class="dashboard-page">
        <!-- SIDEBAR -->
        <aside class="dashboard-sidebar">
            <div class="sidebar-top">
                <h1 class="sidebar-title">لوحة التحكم</h1>
                <ul class="sidebar-menu">
                    <li>
                        <a href="@Url.Action("AddRoleOrView", "Authentication")" class="sidebar-item">
                            <svg viewBox="0 0 24 24"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" /><polyline points="9 22 9 12 15 12 15 22" /></svg>
                            <span>الرجوع</span>
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index", "Administration")" class="sidebar-item">
                            <svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="16" rx="2" /><circle cx="9" cy="10" r="2.5" /><path d="M15 8h4M15 12h4M6 17h12" /></svg>
                            <span>كل الموظفين</span>
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("TrucksIndex", "Administration")" class="sidebar-item">
                            <svg viewBox="0 0 24 24"><rect x="1" y="3" width="15" height="13" /><polygon points="16 8 20 8 23 11 23 16 16 16 8" /><circle cx="5.5" cy="18.5" r="2.5" /><circle cx="18.5" cy="18.5" r="2.5" /></svg>
                            <span>السيارات المسجلة</span>
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("DriversIndex", "Administration")" class="sidebar-item active">
                            <svg viewBox="0 0 24 24"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" /><circle cx="9" cy="7" r="4" /><path d="M23 21v-2a4 4 0 0 0-3-3.87" /><path d="M16 3.13a4 4 0 0 1 0 7.75" /></svg>
                            <span>السائقين المسجلين</span>
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("Suppliers", "Administration")" class="sidebar-item">
                            <svg viewBox="0 0 24 24"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z" /><polyline points="3.27 6.96 12 12.01 20.73 6.96" /><line x1="12" y1="22.08" x2="12" y2="12" /></svg>
                            <span>الموردين</span>
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("SupplierRequests", "Administration")" class="sidebar-item">
                            <svg viewBox="0 0 24 24"><line x1="8" y1="6" x2="21" y2="6" /><line x1="8" y1="12" x2="21" y2="12" /><line x1="8" y1="18" x2="21" y2="18" /><line x1="3" y1="6" x2="3.01" y2="6" /><line x1="3" y1="12" x2="3.01" y2="12" /><line x1="3" y1="18" x2="3.01" y2="18" /></svg>
                            <span>طلبات التوريد</span>
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("TransferRequests", "Administration")" class="sidebar-item">
                            <svg viewBox="0 0 24 24"><line x1="8" y1="6" x2="21" y2="6" /><line x1="8" y1="12" x2="21" y2="12" /><line x1="8" y1="18" x2="21" y2="18" /><line x1="3" y1="6" x2="3.01" y2="6" /><line x1="3" y1="12" x2="3.01" y2="12" /><line x1="3" y1="18" x2="3.01" y2="18" /></svg>
                            <span>طلبات التحويل</span>
                        </a>
                    </li>
                </ul>
            </div>
            <div class="sidebar-user">
                <div class="user-info-group">
                    <div class="user-avatar-box">
                        <svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" /><circle cx="12" cy="7" r="4" /></svg>
                    </div>
                    <div class="user-details">
                        <span class="user-name">إدارة الأرصفة</span>
                        <span class="user-role">مسؤول السائقين</span>
                    </div>
                </div>
            </div>
        </aside>

        <!-- MAIN CONTENT -->
        <main class="dashboard-main">
            <header class="dashboard-header">
                <div class="header-right">
                    <h2 class="header-title">السائقين المسجلين</h2>
                    <div class="header-subtitle">
                        <span class="update-dot"></span>
                        <span>إجمالي السائقين: @(Model?.Count() ?? 0)</span>
                    </div>
                </div>
                <div class="header-left-logos">
                    <img src="~/img/fathallah logo.svg" alt="فتح الله" class="header-logo-fathallah">
                    <img src="~/img/logo.svg" alt="رصيف" class="header-logo-raseef">
                </div>
            </header>

            <div class="table-container">
                <table class="dashboard-table">
                    <thead>
                        <tr>
                            <th>الاسم الكامل</th>
                            <th>رقم الهاتف</th>
                            <th>الرقم القومي</th>
                            <th>نوع السائق</th>
                            <th>تم الإنشاء بواسطة</th>
                            <th>الإجراءات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            foreach (var item in Model)
                            {
                                <tr class="table-row">
                                    <td>@item.FullName</td>
                                    <td>@item.Phone</td>
                                    <td>@item.NationalId</td>
                                    <td>@item.DriverType</td>
                                    <td>@item.CreatedBy</td>
                                    <td>
                                        <a href="@Url.Action("DriverDetails", "Administration", new { id = item.Id })" class="btn-table-details">
                                            <span>تفاصيل</span>
                                            <span>&larr;</span>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="6" class="empty-state">لا يوجد أي سائقين مسجلين حتى الآن</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</body>
</html>
